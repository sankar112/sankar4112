#include <stdio.h>
#include<conio.h>
#include<math.h>
void main()
{
    int a,b,c;
    clrscr();
    printf("enter a,b value");
    scanf("%d%d",&a,&b);
    volume(&a,&b);
    getch();
}
int volume(int*a,int*b)
{
    float pi=5.5;
    int c;
    c=pi**a**a**b;
    printf("%d",c);
    return(0);
}
